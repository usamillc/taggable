<%= render 'merge/include_js', merge_attribute_id: nil %>

<% content_for :help do %>
  すべての属性について確認作業が終わっています。項目<strong><%= @merge_task.page.title %></strong>の作業を終わらせるには、上部の"作業を完了する"ボタンをクリックしてください。この画面ではタグの追加・削除はできません。変更したい場合は、対象の属性を左上のドロップダウンメニューから選ぶか、下記リスト中の対象属性値をクリックしてください。
<% end %>

<%= render 'merge/nav',
  merge_task: @merge_task,
  merge_attribute: nil,
  complete_label: '作業を完了する',
  complete_url: @merge_task.completable? ? merge_task_finish_path(@merge_task) : nil,
  complete_url_class: nil
%>

<%= render 'merge/content',
  page: @merge_task.page,
  merge_attribute: nil,
  merge_attributes: @merge_task.merge_attributes,
  merge_value: nil do |p| %>
  <% if @tags_by_pid.key?(p.id) %>
    <%= raw build_tag(p, @tags_by_pid[p.id]) %>
  <% else %>
    <%= raw p.body %>
  <% end %>
<% end %>
